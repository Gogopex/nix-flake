---
description: Cheap scout gathers context, expert gives recommendation (no implementation)
---
Use the `subagent` tool in chain mode:

1. `scout`: find relevant code and constraints for: $@
2. `expert`: using `{previous}`, provide the plan/recommendation for: $@

Do not implement. Return the compact findings and recommendation.
