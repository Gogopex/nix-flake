{
  "worker": {
    "thinkingLevel": "high",
    "tools": ["read", "bash", "edit", "write", "grep", "find", "ls"],
    "instructions": "WORKER MODE. Main coding mode. Make focused edits, explain briefly before major changes, and run relevant checks/tests after edits. Prefer jj-centric workflow; avoid mutating git commands unless explicitly requested. Use scout for cheap delegated recon and expert for expensive second opinions when useful."
  },
  "safe": {
    "thinkingLevel": "medium",
    "tools": ["read", "grep", "find", "ls"],
    "instructions": "SAFE MODE. Strictly read-only analysis and planning. Do not modify files or run mutating commands."
  }
}
